$kind: http-request
name: Reset Password
method: POST
url: '{{baseUrl}}/passwords/reset'
order: 2000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "password": "newpassword123",
      "token": "{{resetToken}}"
    }
