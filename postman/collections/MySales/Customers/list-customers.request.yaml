$kind: http-request
name: List Customers
method: GET
url: '{{baseUrl}}/customers'
order: 1000
queryParams:
  - key: page
    value: '1'
  - key: limit
    value: '10'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
