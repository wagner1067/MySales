$kind: http-request
name: Show Customer
method: GET
url: '{{baseUrl}}/customers/:id'
order: 2000
pathVariables:
  - key: id
    value: ''
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
