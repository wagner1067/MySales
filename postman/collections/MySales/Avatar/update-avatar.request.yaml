$kind: http-request
name: Update Avatar
method: PATCH
url: '{{baseUrl}}/avatars'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: formdata
  content:
    - key: avatar
      type: file
      src: ''
